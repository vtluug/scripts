network:
  version: 2
  renderer: networkd
  ethernets:
    $INTERFACE:
      dhcp4: no
  bridges:
    br0:
      macaddress: $MAC # Look up in local_hosts
      dhcp4: yes
      interfaces: [$INTERFACE]
